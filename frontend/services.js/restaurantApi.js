class RestaurantApi {

    static url = `${baseURL}/restaurants`

    getRestaurants(){
        
    }
    // fetch requests of API from Rails backend
    // .then (resp => resp.json())
    // . then
}